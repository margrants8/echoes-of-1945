---
import BaseLayout from '../../../components/layout/BaseLayout.astro';
import Header from '../../../components/layout/Header.astro';
import Footer from '../../../components/layout/Footer.astro';
import { getCollection } from 'astro:content';
import { useTranslations, localePath } from '../../../i18n';

const lang = 'zh';
const t = useTranslations(lang);
const p = (path: string) => localePath(path, lang);

const all = await getCollection('warCrimes');
const items = all
  .filter(e => e.id.startsWith('zh/'))
  .sort((a, b) => a.data.date.localeCompare(b.data.date));
---

<BaseLayout title={t.warCrimes.pageTitle} lang={lang}>
  <Header slot="header" lang={lang} pathWithoutLocale="/war-crimes/" />
  <div class="container-wide page-content">
    <div class="page-intro">
      <h1 class="page-title">{t.warCrimes.pageTitle}</h1>
      <p class="page-desc">{t.warCrimes.pageDesc}</p>
    </div>
    <ul class="wc-list">
      {items.map(item => {
        const slug = item.id.replace('zh/', '');
        return (
          <li class="wc-list-item">
            <a href={p(`/war-crimes/${slug}/`)} class="wc-list-link">
              <span class="wc-list-date">{item.data.date.slice(0, 7)}</span>
              <span class="wc-list-title">{item.data.title}</span>
              <span class="wc-list-type">{t.warCrimes.type[item.data.type]}</span>
            </a>
          </li>
        );
      })}
    </ul>
  </div>
  <Footer slot="footer" lang={lang} />
</BaseLayout>

<style>
.page-content { padding-top: var(--space-8); }
.page-intro { max-width: 700px; margin-bottom: var(--space-6); }
.page-title { margin-bottom: var(--space-3); }
.page-desc { color: var(--color-text-muted); line-height: 1.75; }
.wc-list { list-style: none; padding: 0; }
.wc-list-item { border-bottom: 1px solid var(--color-border); }
.wc-list-link {
  display: flex; align-items: baseline; gap: var(--space-3);
  padding: var(--space-3) 0; color: var(--color-text);
  text-decoration: none; transition: color 0.2s; flex-wrap: wrap;
}
.wc-list-link:hover { color: var(--color-accent); text-decoration: none; }
.wc-list-date { font-family: var(--font-mono); font-size: 0.8rem; color: var(--color-text-muted); white-space: nowrap; }
.wc-list-title { font-weight: 600; font-size: 1rem; flex: 1; }
.wc-list-type { font-size: 0.8rem; color: var(--color-text-muted); font-family: var(--font-mono); }
</style>
